# LEXICON - Fullstack React
## Javascript
### Assignment 5

1. Implement a function greet which takes name and message as parameter. Greet the person with the given name and message.

2. Implement a function calculator: take two numbers and operator (+, *, -, /) from user. As per the given operator add, multiply, subtract or divide the numbers and return the result.

3. Fizz Buzz game:
    - Read a number from user.
    - If number is divisible by 3 print “Fizz”
    - If number is divisible by 5 print “Buzz”
    - If number is divisible by 3 and 5 both print “Fizz Buzz”
    - Otherwise print the number


*Note - run npm install*

